#pragma once

#include "draft/core/entity.hpp"
#include "draft/core/scene.hpp"

namespace Draft {
    /// Holds physics tools, like easy constraints
    namespace Toolbox {
        // Entity constrain_distance(Scene& scene, Entity a, Entity b);
    }
}