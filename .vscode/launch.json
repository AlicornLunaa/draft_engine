{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/bin/space_game",
            "args": [],
            "cwd": "${workspaceFolder}/build/bin",
            "preLaunchTask": "CMake: build"
        }
    ]
}